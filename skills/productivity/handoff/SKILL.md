---
name: handoff
description: 将当前对话压缩成交接文档，供另一个智能体继续工作。
argument-hint: "下一次会话将用于什么？"
---

写一份交接文档，总结当前对话，使一个新的智能体可以继续工作。保存到由 `mktemp -t handoff-XXXXXX.md` 产生的路径（在写入之前读取文件）。

建议下一次会话要使用的技能（如果有）。

不要重复其他产出物（PRD、计划、ADR、Issue、提交、差异）中已捕获的内容。改为通过路径或 URL 引用它们。

如果用户传递了参数，将它们视为下一次会话将聚焦的内容的描述，并相应地定制文档。
